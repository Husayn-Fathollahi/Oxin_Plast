/**
 * Admin — Per-page SEO configuration.
 * Edit meta titles, descriptions, OG images, and canonical URLs per page/route.
 * TODO: table of SEO configs fetched from /api/v1/seo.
 */
export default function AdminSeoPage() {
  return (
    <div>
      <h1 className="mb-6 text-2xl font-semibold">SEO Settings</h1>
      {/* TODO: <SeoConfigTable /> */}
    </div>
  );
}
