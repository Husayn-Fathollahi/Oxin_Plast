// Header and Footer are rendered by the parent [locale]/layout.tsx.
// This layout exists only to be a placeholder for future about-specific layout needs.
export default function AboutLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
